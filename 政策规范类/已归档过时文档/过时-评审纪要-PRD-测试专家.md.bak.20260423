# PRD可测试性评审纪要 - 测试专家视角

**评审日期**：2026-04-22
**评审角色**：测试专家
**评审对象**：PRD-量化交易系统v1.0.md
**评审标准**：UNI-MANDATORY-001 v2.6.0

---

## 一、评审结论

**总体判定**：⚠️ 有条件通过（需整改后方可进入开发阶段）

**核心问题**：PRD在API幂等性、测试覆盖率基线、测试金字塔分层、性能测试指标量化方面存在缺失，与UNI-MANDATORY-001标准存在偏差，需补充完善。

---

## 二、详细评审结果

### 2.1 API可测试性评审

#### 符合项 ✅

| 评审项 | 描述 | PRD章节 |
|--------|------|---------|
| 请求/响应格式 | 所有API均有完整的请求/响应示例 | 4.2 |
| gRPC定义 | Proto文件完整，包含所有枚举和消息类型 | 4.3 |
| 错误码规范 | code=0成功, code>0业务错误, code<0系统错误 | 4.1 |
| 分页规范 | 查询API支持page/page_size参数 | 4.2 |

#### 缺失项 ❌

| 评审项 | 标准要求 | 当前状态 | 影响 |
|--------|----------|----------|------|
| **幂等键** | L1-006: 所有状态修改API必须支持Idempotency-Key | PRD仅在client_order_id中隐含，未明确声明幂等键机制 | 无法有效测试重复提交场景 |
| **限流响应头** | L1-006: 响应必须包含X-RateLimit-Limit/Remaining/Reset | API响应格式中未定义 | 无法验证限流功能 |
| **CSRF Token校验** | L1-006: 状态修改API必须校验CSRF Token | 未提及 | 安全测试用例无法设计 |
| **排序参数** | 查询API应支持sort字段 | 仅提到筛选，未定义排序 | 排序回归测试缺失 |

#### 修复建议
```
1. 在所有状态修改API（POST/PUT/DELETE）中增加Idempotency-Key头说明
2. 在响应格式中增加限流头定义：
   X-RateLimit-Limit: 1000
   X-RateLimit-Remaining: 999
   X-RateLimit-Reset: 1713768600
3. 在API文档中增加CSRF校验说明
```

---

### 2.2 验收标准可测试性评审

#### 符合项 ✅

| 评审项 | 描述 |
|--------|------|
| BDD格式 | 验收标准采用Given/When/Then格式，描述清晰 |
| 条件覆盖 | 涵盖正常路径和异常路径（如资金不足、持仓超限） |
| 性能指标 | 明确P99≤10ms、≤5ms等量化指标 |

#### 缺失项 ❌

| 评审项 | 标准要求 | 当前状态 | 影响 |
|--------|----------|----------|------|
| **单元测试覆盖率基线** | L1-010: 安全核心模块≥85%分支覆盖，普通模块≥80%行覆盖 | 未定义 | 无法建立CI门禁基线 |
| **突变测试要求** | L1-010: 核心业务逻辑必须启用pitest | 未提及 | 无法验证测试有效性 |
| **集成测试要求** | 应定义各模块间的集成测试场景 | 仅列出单元验收标准 | 接口集成测试缺失 |
| **E2E测试场景** | 应定义关键业务流程的端到端测试 | 未提及 | 全链路测试缺失 |

#### 缺失的验收标准

```markdown
❌ 缺失：订单状态机 - 所有状态转换的覆盖测试（CREATED→REJECTED边界）
❌ 缺失：改单场景 - 乐观锁冲突时的重试逻辑
❌ 缺失：熔断恢复 - 人工确认后系统行为验证
❌ 缺失：行情中断恢复 - 行情源切换后的数据一致性
❌ 缺失：日终清算 - 跨日后系统状态验证
```

#### 修复建议

建议在Section 6增加测试分层说明：

```markdown
### 6.X 测试分层要求

#### 单元测试（开发阶段）
- 行覆盖率 ≥ 80%（普通模块）
- 分支覆盖率 ≥ 85%（BIZ-TRADE/BIZ-RISK等安全核心模块）
- 突变测试得分 ≥ 70%（订单计算/风控规则/盈亏计算）

#### 集成测试（提测阶段）
- 模块间API调用测试（同步/异步）
- 数据库事务一致性测试
- 消息队列事件消费测试

#### E2E测试（发布前）
- 完整下单流程测试（选标的→下单→成交→持仓更新）
- 风控规则触发全流程
- 熔断触发与恢复流程
```

---

### 2.3 性能测试可测试性评审

#### 符合项 ✅

| 指标 | 值 | PRD章节 |
|------|-----|---------|
| 订单下单P99 | ≤10ms | 2.2.1 |
| 撤单响应 | ≤5ms | 2.2.3 |
| 成交回报推送 | ≤5ms | 2.2.6 |
| 行情延迟 | <1秒 | 2.4.1 |

#### 缺失项 ❌

| 评审项 | 说明 | 影响 |
|--------|------|------|
| **测试前提定义** | 性能指标对应的测试环境/数据量级未定义 | 测试结果无法标准化 |
| **P99计算方式** | 未说明采样方法（滑动窗口/固定窗口） | 不同工具结果可能不一致 |
| **性能测试场景** | 未定义基准测试/负载测试/压力测试场景 | 测试执行缺乏指导 |
| **容量规划** | 未给出预期QPS和并发数 | 无法设计压力测试参数 |

#### 修复建议

建议增加性能测试说明：

```markdown
### 性能测试基准
- 测试环境：4核CPU/16GB内存/SSD，同等规格容器
- 测试数据：1000个账户，每账户50持仓，10万历史订单
- 预热：1000请求
- 采样：滑动窗口10秒，P99计算
- 目标QPS：1000（交易下单）

### 性能测试场景
1. 基准测试：单用户顺序下单，验证功能正确性
2. 负载测试：100并发，验证吞吐量和延迟
3. 压力测试：逐步增加至2000并发，寻找瓶颈
4. 稳定性测试：持续30分钟，验证无内存泄漏
```

---

### 2.4 安全测试可测试性评审

#### 符合项 ✅

| 评审项 | 描述 | PRD章节 |
|--------|------|---------|
| JWT算法 | RS256/ES256强制 | 4.1 |
| 审计日志 | 包含before_image/after_image | 2.3.5 |
| 敏感数据加密 | ChaCha20-Poly1305或AES-256 | L1-004 |

#### 缺失项 ❌

| 安全威胁 | 标准要求测试验证 | PRD是否覆盖 |
|----------|------------------|-------------|
| SQL注入 | 自动化SQLMap测试 | ❌ 未提及 |
| XSS | 自动化扫描+输出编码白盒测试 | ❌ 未提及 |
| CSRF | Token验证测试覆盖所有状态修改API | ❌ 未提及 |
| JWT | 算法验证+刷新+吊销+密钥轮换测试 | ❌ 未提及 |
| IDOR | 水平越权测试（不同用户互访返回403） | ❌ 未提及 |
| 暴力破解 | 5次失败后账户锁定测试 | ❌ 未提及 |

#### 修复建议

建议在Section 6增加安全测试要求：

```markdown
### 6.X 安全测试要求

#### 自动化安全扫描（CI强制）
- SAST：使用SonarQube/Checkmarx扫描代码漏洞
- DAST：使用SQLMap/Burp Suite进行动态扫描
- High/Critical级别漏洞禁止上线

#### 安全测试用例
1. SQL注入：验证所有用户输入均使用参数化查询
2. XSS：验证输出均做转义处理
3. CSRF：验证状态修改API均有Token校验
4. JWT：验证RS256/ES256算法，拒绝HS256
5. IDOR：验证用户A无法访问用户B的资源（返回403）
6. 暴力破解：验证5次失败后账户锁定5分钟
```

---

### 2.5 数据模型可测试性评审

#### 符合项 ✅

| 评审项 | 描述 |
|--------|------|
| 字段类型 | 所有字段类型定义清晰 |
| 约束条件 | NOT NULL/DEFAULT/PK/FK明确定义 |
| 索引 | 关键查询字段均有索引 |

#### 缺失项 ❌

| 评审项 | 影响 |
|--------|------|
| **测试数据构造** | 无唯一性约束辅助字段（如UNIQUE INDEX），测试数据构造困难 |
| **状态机边界** | 订单状态机缺少错误状态（如TIMEOUT）的转换定义，无法测试超时场景 |
| **负向测试数据** | 未提供无效symbol/账户的测试数据规格 |

---

## 三、整改清单

| 优先级 | 问题 | 建议修复方式 | 修复章节 |
|--------|------|--------------|----------|
| **P0** | 幂等键机制缺失 | 在下单/改单/撤单API中增加Idempotency-Key头说明 | 4.2 |
| **P0** | 安全测试要求缺失 | 增加安全测试用例章节 | 6.X |
| **P0** | 测试覆盖率基线未定义 | 明确单元测试覆盖率要求 | 6.X |
| **P1** | 限流响应头缺失 | 在响应格式中增加X-RateLimit-*头 | 4.2 |
| **P1** | 性能测试前提缺失 | 定义测试环境/数据量级 | 6.X |
| **P1** | E2E测试场景缺失 | 增加关键业务流程E2E测试用例 | 6.X |
| **P2** | 排序参数未定义 | 查询API增加sort参数说明 | 4.2 |
| **P2** | 状态机错误场景缺失 | 补充订单状态机TIMEOUT状态 | 2.3.1 |

---

## 四、测试策略建议

### 4.1 测试金字塔分层

```
        /\
       /  \      E2E Tests (关键业务流程)
      /----\     数量：10-20个场景
     /      \    
    /--------\   Integration Tests (模块间接口)
   /          \   数量：50-100个用例
  /------------\  
 /              \ Unit Tests (各模块核心逻辑)
/                数量：500+用例，覆盖率≥80%
```

### 4.2 测试数据策略

- 使用Faker生成真实感测试数据
- 建立测试数据Fixture库，支持快速回放
- 使用数据库事务回滚保证测试间隔离

### 4.3 自动化测试工具建议

| 层级 | 工具 | 说明 |
|------|------|------|
| 单元测试 | JUnit 5 + Mockito | Java后端 |
| 单元测试 | Jest + Testing Library | React前端 |
| 集成测试 | TestContainers | 数据库/中间件 |
| API测试 | RestAssured + REST Client | REST/gRPC |
| 性能测试 | k6 | 符合标准推荐 |
| 安全测试 | SQLMap + OWASP ZAP | 自动化扫描 |
| 突变测试 | Pitest | Java mutation testing |

---

## 五、评审签字

| 角色 | 评审意见 | 签字 | 日期 |
|------|----------|------|------|
| 测试专家 | 条件通过，需整改P0问题 | [待签字] | 2026-04-22 |

---

*评审完成时间：2026-04-22 07:45*