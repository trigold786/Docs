# PRD评审纪要 - 前端开发专家

**评审时间**：2026-04-22
**评审人**：前端开发专家
**评审文档**：PRD-量化交易系统v1.0.md
**参考标准**：UNI-MANDATORY-001 v2.6.0

---

## 一、评审结论

**评审结论**：建议修改后通过

PRD整体前端规格描述较为完整，技术选型合理，但存在以下需要明确或补充的问题，建议在开发前完成对齐。

---

## 二、符合标准项

### 2.1 符合L1强制标准项

| 标准条款 | 要求 | PRD现状 | 评估 |
|---------|------|---------|------|
| L1-005 安全基线 | XSS防护：React禁止dangerouslySetInnerHTML | 5.4技术要求选用React 18，未提及dangerouslySetInnerHTML使用 | 符合（默认React安全实践） |
| L1-005 安全基线 | CSRF Token验证 | 4.1 API规格提到JWT认证，未明确CSRF Token要求 | 待明确 |
| L1-005 安全基线 | JWT算法RS256/ES256 | 4.1明确要求"算法RS256/ES256" | 符合 |
| L1-006 接口规范 | 响应格式{code, message, data, request_id} | 4.1明确定义 | 符合 |
| L1-006 接口规范 | 分页元数据total/page/page_size/has_next | 4.2.x查询接口提到page/page_size，但未说明response必须包含分页元数据 | 待明确 |
| L1-008 自建UI系统优先 | 优先使用自建UI系统 | 5.4明确"UI组件库：自建组件库（须含Design Tokens）" | 符合 |

### 2.2 符合L2标准项

| 标准条款 | 要求 | PRD现状 | 评估 |
|---------|------|---------|------|
| L2-007 多端适配优先 | BFF层职责：多端适配/接口聚合/协议转换/认证透传 | 7.1依赖图提及API Gateway/BFF，但未详细定义前端BFF职责 | 待明确 |

---

## 三、待确认/补充问题

### 3.1 【高优先级】前端架构边界问题

**问题描述**：
- PRD 7.1依赖图显示"Frontend (React) → API Gateway/BFF → BIZ-xxx"
- 但未明确定义BFF层的前端职责边界，特别是：
  - 前端是否直连BIZ-RISK进行风控预检？还是必须通过BFF？
  - WebSocket连接是直连BIZ-MARKET还是通过BFF？
  - 告警推送SSE的连接路径？

**影响**：
- 前端架构设计依赖此边界定义
- 安全审计需要明确数据流向

**建议**：
在PRD中补充"前端与BFF的接口协议"章节，明确哪些API必须经过BFF，哪些可以直连。

---

### 3.2 【高优先级】实时数据处理机制缺失

**问题描述**：
- 5.4技术要求提到">100数据点/秒使用requestAnimationFrame批处理"
- 但未定义：
  - 数据源积压（Buffer）策略
  - 低优先级数据丢弃策略
  - 页面后台时的数据处理策略（Page Visibility API）
  - 数据同步失败的重试机制

**影响**：
- 高频行情场景下可能导致页面卡顿
- 网络波动时数据一致性无法保证

**建议**：
补充"前端实时数据处理规范"，定义：
1. 数据更新频率分级（行情>10Hz、成交>1Hz、告警<0.1Hz）
2. 批处理阈值和刷新策略
3. 断线重连的指数退避参数

---

### 3.3 【高优先级】错误处理规范缺失

**问题描述**：
- PRD定义了错误码规范（code>0业务错误，code<0系统错误）
- 但未定义：
  - 前端如何区分可重试错误和不可重试错误
  - 429限流时前端的退避重试策略
  - 网络超时前端的默认重试次数
  - 服务不可用时的降级方案（显示缓存数据/提示用户）

**影响**：
- 不同前端开发者可能有不同的错误处理策略
- 用户体验不一致

**建议**：
在API规格章节补充"前端错误处理规范"，至少包含：
1. 错误分类（网络错误/业务错误/系统错误）
2. 重试策略（指数退避，最大3次）
3. 降级fallback行为

---

### 3.4 【中优先级】WebSocket连接管理缺失

**问题描述**：
- 4.2.3行情模块定义WebSocket `/v1/market/stream`
- 5.2.4告警列表组件要求"WebSocket实时更新"
- 但未定义：
  - 连接建立/断开的生命周期管理
  - 多Tab同时在线时的数据同步策略
  - 心跳保活机制（心跳间隔/超时阈值）
  - 服务端重启时的无缝重连

**影响**：
- 网络波动时可能产生数据不一致
- 多Tab场景可能出现状态冲突

**建议**：
补充WebSocket连接管理规范，至少包含：
1. 心跳间隔建议值（如30s）
2. 断连判定时间（如60s无响应）
3. 重连指数退避参数

---

### 3.5 【中优先级】状态管理规范缺失

**问题描述**：
- 5.4技术要求提到"状态管理：Redux Toolkit / Zustand"
- 但未定义：
  - 状态分类（Server State vs Client State）
  - 缓存策略（哪些数据需要缓存？缓存有效期？）
  - 多用户/多账户切换时的状态隔离方案
  - 敏感状态（如登录态）的安全存储要求

**影响**：
- 状态管理混乱可能导致数据不一致
- 安全风险（如Token存储不当）

**建议**：
在技术要求章节补充"前端状态管理规范"，明确：
1. Server State使用TanStack Query/SWR等方案
2. Client State使用Zustand
3. Token存储：Memory优先，HttpOnly Cookie备选
4. 账户切换时的状态清理流程

---

### 3.6 【中优先级】组件规格不完整

**问题描述**：
- 5.2定义组件规格时，OrderInput组件状态定义不够完整：
  - 未定义"riskConfirming"状态（风控预检通过后等待用户确认的状态）
  - 未定义组件的disabled时间段（如风控检查中不可重复提交）

**影响**：
- 前端组件实现可能出现状态遗漏
- 重复提交风险

**建议**：
补充OrderInput组件的完整状态机和每个状态的UI行为。

---

### 3.7 【低优先级】可访问性要求缺失

**问题描述**：
- L1-008要求自建UI系统须满足WCAG 2.1 AA可访问性要求
- PRD未提及可访问性相关要求

**影响**：
- 可能不符合合规要求
- 障碍用户无法使用系统

**建议**：
在验收标准中补充可访问性要求：
1. 键盘导航支持
2. 屏幕阅读器兼容
3. 颜色对比度要求（4.5:1文本，3:1大文本）
4. focus可见性要求

---

### 3.8 【低优先级】国际化要求缺失

**问题描述**：
- PRD全部使用中文，但量化交易系统可能面向国际用户
- 未定义i18n策略

**建议**：
如无国际化需求，明确标注"本期不支持多语言"；如有需求，补充i18n架构设计。

---

## 四、建议补充的规范

基于以上问题，建议在PRD中补充以下前端相关规范：

| 规范名称 | 位置 | 优先级 |
|---------|------|--------|
| 前端与BFF接口协议 | 第四章API规格 | P0 |
| 前端实时数据处理规范 | 第五章技术要求 | P0 |
| 前端错误处理规范 | 第四章API规格 | P0 |
| WebSocket连接管理规范 | 第五章技术要求 | P1 |
| 前端状态管理规范 | 第五章技术要求 | P1 |
| 前端可访问性规范 | 第五章技术要求 | P2 |

---

## 五、总结

| 评审维度 | 结论 | 说明 |
|---------|------|------|
| 技术选型 | 通过 | React 18 + TypeScript + 自建组件库 + Zustand符合现代前端实践 |
| 安全基线 | 基本通过 | JWT算法、数据格式符合要求，但CSRF Token需前端明确实现 |
| 性能要求 | 基本通过 | FCP/LCP/CLS/INP指标合理，但实时数据处理机制需补充 |
| 组件规格 | 待补充 | 核心组件定义完整，但状态机不够细致 |
| 架构边界 | 待明确 | BFF与前端职责边界需要明确定义 |
| 可访问性 | 待补充 | WCAG要求需在PRD中体现 |

**建议行动**：
1. 补充上述P0优先级的规范定义
2. 组织前端团队对架构边界问题进行评审确认
3. 在开发阶段开始前完成可访问性要求的对齐

---

*评审人：前端开发专家*
*评审日期：2026-04-22*