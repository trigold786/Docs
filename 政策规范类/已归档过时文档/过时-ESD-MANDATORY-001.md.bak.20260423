# 软件产品研发必读标准
## Enterprise Software Product R&D Mandatory Standards

**版本：v1.0 | 日期：2026-04-19**
**性质：本文档为产品研发强制遵从标准，编号：ESD-MANDATORY-001**

---

## ⚠️ 强制声明

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                                                                             │
│   本文档是所有软件产品研发工作的最低强制要求。                                │
│                                                                             │
│   适用范围：                                                                │
│   所有软件开发、系统设计、架构设计、测试、运维、安全合规等工作，              │
│   无论项目规模大小、技术栈类型，均须 100% 遵从本文档。                      │
│                                                                             │
│   强制等级：                                                                │
│   ████████████████████ 强制（MANDATORY）████████████████████              │
│                                                                             │
│   违反任何一条 → 产出视为无效 → 必须整改 → 重新审查 → 无例外豁免            │
│                                                                             │
│   本文档编号：L1-CORE                                                       │
│   上层文件：本文档为第一层（L1），是唯一核心标准，                            │
│   下层 L2/L3/L4 文件不得与本文档冲突，冲突时以本文档为准。                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 第一层：架构红线（10 条，0 例外）

### 一、架构分层（强制）

```
层级自下而上调用，禁止逆向：

  L4 基础设施层：Kubernetes · PostgreSQL · Redis · Kafka · OSS
      ↑
  L3 基础功能模块层：UAM（账户）│ SMS（安全）│ PAY（支付）│
                       MSG · CFG · LOG · CACHE · SEARCH · FILE
      ↑
  L2 业务功能模块层：BIZ-MEM · BIZ-ORD · BIZ-PDT · BIZ-MKT
      ↑
  L1 聚合模块层：AGG-USER · AGG-TRADE · AGG-CONTENT

规则：
  ① L4 禁止调用上层
  ② L3 禁止调用 L2/L1
  ③ L2 禁止调用其他业务模块
  ④ L1 通过事件总线通信，禁止直接同步调用
  ⑤ 依赖层级 ≤ 2
```

### 二、依赖管控（强制）

```
  ❌ 禁止循环依赖（CI 必须检测，不通过禁止合并）
  ❌ 禁止跨业务线依赖
  ❌ 禁止反向依赖（高层依赖低层具体实现）
  ✅ 构造函数注入（首选）
  ✅ 依赖层级 ≤ 2
```

### 三、三大基础模块（强制接入或实现）

| 模块 | 代码 | 必须提供的能力 |
|------|------|--------------|
| 用户账户管理 | UAM | 注册 / 登录 / 会话Token / SSO / MFA |
| 系统安全管理 | SMS | 权限RBAC / 审计日志 / 数据加密 / 合规 |
| 付费支付 | PAY | 支付通道 / 订单 / 订阅 / 对账 |

所有业务模块必须通过统一 SDK 调用基础模块，禁止直连对方数据库。

### 四、数据边界（强制）

```
  ① 每个模块只能读写自己的数据库 schema/collection
  ② 跨模块数据共享必须通过 API 或事件总线
  ③ 所有表必须包含：created_at, updated_at, created_by, updated_by
  ④ 密码禁止明文存储（必须 Argon2id 或 bcrypt）
  ⑤ 敏感数据 ChaCha20-Poly1305 或 AES-256 加密存储
```

### 五、安全基线（强制）

| 威胁 | 最低要求 |
|------|---------|
| SQL注入 | 参数化查询（`?` 占位符），禁止字符串拼接 |
| XSS | 输出转义 + CSP 响应头 |
| CSRF | Token 验证（32字节随机数），Session HttpOnly |
| 暴力破解 | IP 限速 + 账户锁定（5次失败锁5分钟） |
| 传输 | 全站 HTTPS（TLS 1.2+），HSTS 头 |
| 敏感数据 | 配置中心加密存储，运行时 KMS 解密 |
| 审计 | 所有状态变更写日志，不可删除只可追加 |

### 六、接口规范（强制）

```
  ① RESTful API，路径版本化（/v1/ /v2/）
  ② 认证：Bearer Token（JWT）
  ③ 格式：application/json
  ④ 响应：{ code, message, data, request_id }
  ⑤ 废弃：至少提前 6 个月通知
  ⑥ 状态修改 API 必须校验 CSRF Token
```

### 七、事件通信（强制）

```
  ① 同步 SLA：P99 ≤ 100ms，可用性 ≥ 99.9%
  ② 事件格式：event_id + event_type + module_from + module_to + payload + timestamp + idempotency_key
  ③ 持久化：Kafka 副本 ≥ 3
  ④ 消费者必须幂等
  ⑤ 失败事件进入 DLQ
```

### 八、命名规范（强制）

```
模块命名：
  基础模块：BASE-{NAME}          （例：BASE-UAM）
  业务模块：BIZ-{DOMAIN}-{NAME}  （例：BIZ-MEM）
  聚合模块：AGG-{DOMAIN}          （例：AGG-USER）

Git 仓库：{org}/{module}-{name}

Commit：
  <type>(<scope>): <subject>
  type：feat | fix | docs | style | refactor | test | chore
```

### 九、版本管理（强制）

```
  ① SemVer：MAJOR.MINOR.PATCH
      MAJOR：不兼容变更
      MINOR：向后兼容功能新增
      PATCH：向后兼容 bug 修复
  ② 基础模块不得破坏向后兼容
  ③ 主版本升级必须提供迁移文档
```

### 十、质量门槛（强制）

```
  ① 单元测试覆盖率 ≥ 80%（CI 不通过禁止合并）
  ② 所有 Merge/PR 至少 1 人审批
  ③ 重大变更必须架构评审
  ④ 安全问题：SAST+DAST 扫描，High/Critical 禁止上线
  ⑤ 灰度发布：10% → 30% → 50% → 100%
  ⑥ 自动回滚：错误率 > 1% 或 P99 > 500ms
```

---

## 第二层：架构设计规范

### 11. 架构设计评审门槛

```
必须评审的情形：
  □ 新增基础模块
  □ 跨业务线调用关系变更
  □ 引入新技术栈或中间件
  □ 数据库选型变更
  □ 安全策略变更
评审：ASG + 业务线技术负责人，7 工作日内结论
```

### 12. 模块划分原则

```
  ① 按业务能力（Business Capability）划分
  ② 模块边界 = DDD 边界上下文
  粒度：一个模块包含 3-10 个强相关 API
  每个模块必须文档化：职责 / API 清单 / 依赖 / 数据 Owner / SLA
```

### 13. 基础模块复用评估（新增业务强制步骤）

```
  □ 账户能力：现有 BASE-UAM 能满足吗？
  □ 安全能力：现有 BASE-SMS 能满足吗？
  □ 支付能力：现有 BASE-PAY 能满足吗？
  自建判断：开发成本 > 复用成本 × 复用业务线数量 → 不值得复用
```

### 14. 容错与韧性设计

```
每个模块必须设计：
  ① 超时控制（默认 30s）
  ② 重试（最多 3 次，指数退避）
  ③ 熔断降级
  ④ 限流（超出处理能力时友好拒绝）
```

### 15. 可观测性设计

```
每个模块必须定义：
  Metrics：QPS / 错误率 / P99延迟 / 资源使用率
  Tracing：trace_id 从入口透传到出口
  Logging：JSON格式，含 timestamp/trace_id/module/level/message
           禁止日志中出现：密码 / Token / 密钥 / 手机号 / 身份证
  Health：/healthz（存活） + /readyz（就绪，含依赖检查）
```

### 16. API 版本规划

```
  ① URL 版本化：/v1/, /v2/
  ② 废弃提前 6 个月通知
  ③ 过渡期 6 个月
  响应头：X-API-Version / X-Request-Id / X-RateLimit-Remaining
```

### 17. 等保定级预测

```
定级三要素：
  受侵害客体：公民合法权益 → Level 1
             社会秩序、公共利益 → Level 2
             国家安全 → Level 3+
  侵害程度：造成一定影响 → Level 1
           造成较大影响 → Level 2
           造成严重/特别严重影响 → Level 3+

典型定级：
  Level 1：企业内部资讯系统、工具类应用
  Level 2：用户管理系统、API 密钥管理系统 ← 本体系默认目标
  Level 3：支付系统、核心交易平台

定级流程：识别客体 → 评估程度 → 确定等级 → 编写报告 → 专家评审（Level 2+）
```

### 18. 等保二级技术要求清单（强制接入）

```
【身份鉴别】（8项）
  □ 强壮密码策略（大小写+数字+特殊字符，≥8位）
  □ 多因素认证支持（MFA/OTP/生物识别）
  □ 每个用户唯一标识（user_id）
  □ 禁止共享账号
  □ 密码 Argon2id/bcrypt 存储
  □ 登录失败锁定（5次失败锁5分钟）
  □ 会话超时登出（≤30分钟idle）
  □ 审计日志记录登录行为

【访问控制】（6项）
  □ 最小权限（RBAC/ABAC）
  □ 管理员权限分离（不用root运行服务）
  □ 敏感操作二次确认
  □ 禁止访问控制权限篡改
  □ 账户分配遵循最小权限
  □ 定期审查账户权限（≥90天）

【安全审计】（5项）
  □ 记录所有状态变更操作
  □ 审计日志不可删除（append-only）
  □ 日志保留≥180天
  □ 日志防篡改（哈希链）
  □ 审计记录含：时间/用户/操作/结果/客户端IP

【数据保护】（6项）
  □ TLS 1.2+ 传输加密
  □ ChaCha20-Poly1305/AES-256 存储加密
  □ 敏感数据脱敏展示
  □ 重要数据备份（≥每日增量，≥每周全量）
  □ 远程数据销毁能力
  □ 数据删除记录审计日志

【网络保护】（5项）
  □ HTTPS 全站强制
  □ 安全响应头完整（CSP/X-Frame-Options/HSTS）
  □ 接口限速防 DDoS
  □ 网络边界防火墙隔离
  □ 无线网络 WPA2/WPA3

【主机保护】（6项）
  □ 非 root 用户运行
  □ 最小系统调用过滤
  □ 独立数据目录权限（chmod 700）
  □ 系统安全补丁≤7天更新
  □ 禁止开放不必要端口
  □ 日志集中收集分析
```

### 19. 等保三级增强要求

```
相比二级额外要求：
  【身份鉴别】+ 双重因素强制 / 登录异常检测 / 生物识别第二因素 / 重要操作二次认证
  【访问控制】+ 三权分立（系统管理员/安全管理员/审计管理员）/ 强制访问控制（MAC）
  【安全审计】+ 实时监控告警 / 关联分析 / 年度第三方审计
  【数据保护】+ 数据分类分级 / 核心数据不出境 / KMS/HSM密钥管理 / 数据水印
  【网络保护】+ WAF抗APT / IDS入侵检测 / DMZ分区隔离
  【入侵防范】+ IDS / 半年度自动化渗透 / ≤24小时0day响应
```

---

## 第三层：开发实施规范

### 20. 编码通用原则

```
  ① 命名即注释：变量/函数名必须自解释
  ② 单一职责：每个函数只做一件事
  ③ 异常处理：永远不吞异常（至少记录日志）
  ④ 硬编码禁止：配置外置，禁止魔法数字
  ⑤ 资源释放：finally/defer 中释放
  ⑥ 边界检查：外部输入必须校验（类型/长度/范围）
  ⑦ 日志规范：INFO记录流程，WARN记录可恢复异常，ERROR记录故障
```

### 21. 各语言编码规范

```
【Go】（Uber Go 规范）
  □ 永远不忽略 error
  □ 每个函数传递 context.Context
  □ 依赖注入通过 wire
  □ golangci-lint 必须通过

【Python】（PEP 8 + Black + mypy）
  □ 所有函数有 Type Hints
  □ Black 格式化
  □ flake8 + mypy 必须通过

【TypeScript】（Airbnb + TypeScript）
  □ 强制 TypeScript，禁止 any
  □ async/await，禁止 Promise 嵌套
  □ ESLint + Prettier 必须通过

【Java】（Alibaba Java 规范）
  □ 线程安全集合优先
  □ Lint 必须通过

【Swift / Kotlin】
  □ 官方编码规范
  □ 值类型优先 / 禁止 !! / 协程异步
```

### 22. 安全编码（所有语言通用）

```
  ✅ 参数化查询 / ORM
  ❌ 禁止字符串拼接 SQL / eval() / shell=True+用户输入拼接
  ✅ 模板引擎默认转义 / React 默认防护
  ❌ 禁止在日志中记录密码/Token/密钥
  ❌ 禁止在 Git 中提交密钥
  ❌ 禁止在 URL 参数中传递敏感数据
```

### 23. 代码注释规范（强制）

```
注释原则：
  ① 代码即文档：好的代码本身应自解释
  ② 注释必须与代码同步更新
  ③ 禁止无意义注释（// nothing / // asdf）
  ④ 禁止死代码（已注释掉的代码应及时删除）

【必须注释的情况】

  ① 文件头（每个源文件必须包含）
     /*
      * 文件名：{filename}
      * 功能：{一句话描述}
      * 作者：{author}
      * 日期：{YYYY-MM-DD}
      */

  ② 导出函数（所有 public 函数必须包含）
     /**
      * 函数名：{name}
      * 功能：{详细描述}
      * 参数：{name} {类型} {含义}
      * 返回：{类型} {含义}
      * 异常：{可能的异常及含义}
      */

  ③ 复杂业务逻辑（超过3层嵌套/关键算法/状态机/边界条件）
     // BIZ: 为什么这样写（业务原因），而非 // 做判断

  ④ 常量/枚举
     MAX_TOKEN_TTL_SEC = 86400  # Token最大生存期，单位秒
     // 禁止：MAGIC_NUMBER = 86400  # 常量

【禁止注释】
  ❌ 显而易见的行为（如：i += 1  # i自增）
  ❌ 与代码矛盾的注释（发现时立即修复）

【TODO 注释】
  // TODO(author): {描述} + issue编号
  示例：// TODO(zhangsan): v2.0支持批量操作，issue#123
```

### 24. API 端点注释（OpenAPI 3.0）

```
所有 API 端点必须包含 OpenAPI 注解：

  /**
   * 用户注册
   * @param {object} body - 注册请求体
   * @param {string} body.username - 用户名（3-64字符）
   * @param {string} body.password - 密码（8位以上）
   * @returns {object} { code: 0, data: { user_id } }
   * @throws {400} 参数校验失败
   * @throws {409} 用户名已存在
   */
  POST /uam/v1/accounts/register
```

### 25. 数据库变更注释

```
迁移脚本必须包含：

  -- 文件名：V{version}__{description}.sql
  -- 作者：{author}
  -- 日期：{YYYY-MM-DD}
  -- 功能：{详细说明}
  -- 回滚：{回滚方案说明}
```

### 26. Git 规范

```
分支命名：
  master / develop（受保护）
  feature/{issue-id}-{short-desc}
  fix/{issue-id}-{short-desc}
  release/v{version}
  hotfix/{issue-id}-{short-desc}

Commit：
  <type>(<scope>): <subject>
  feat(uam): add SMS login support
  fix(pay): resolve double deduction on timeout
  docs(api): update endpoint documentation
  refactor(sms): migrate to new RBAC model
  test(uam): add login rate limit tests
  chore(deps): upgrade argon2 to v2.1

工作流：feature → develop → release → master
```

### 27. 提交前检查清单

```
  1. 格式化：black / gofmt / npm run format
  2. Lint：flake8 / golangci-lint / eslint
  3. 类型检查：mypy / tsc --noEmit
  4. 单元测试：pytest / go test
  5. 安全：禁止密钥提交（git hooks / detect-secrets）

CI 门控：构建 → 测试 → Lint → 安全扫描 → 镜像构建
          任一失败 → 禁止合并
```

### 28. 测试规范

```
三层测试金字塔：
        E2E 测试（少量，核心路径 100%）
        集成测试（中等，API 交互）
        单元测试（大量，≥80% 覆盖率）
```

### 29. TDD 红绿重构循环

```
  RED（红）：写失败测试（明确要实现的功能）→ git commit "test: ..."
  GREEN（绿）：写最小代码通过 → git commit "feat: ..."
  REFACTOR（重构）：改善代码结构 → git commit "refactor: ..."

约束：
  ① 不跳步（不先写代码再补测试）
  ② 不在 RED 时写多余测试
  ③ 不在 GREEN 时重构
```

### 30. 调试方法论（系统性四阶段）

```
铁律：未完成 Phase 1，不得提出任何修复方案。

Phase 1（根因调查）：仔细阅读错误 / 可靠复现 / 检查最近变更 / 追踪数据流
Phase 2（模式分析）：找相似正常代码 / 对比参考 / 识别差异 / 理解依赖
Phase 3（假设与测试）：陈述假设 / 最小化测试 / 3次失败后进入架构质疑
Phase 4（修复验证）：创建回归测试 / 修复根因 / 验证无回归 / 提交

⚠️ 3 次失败后：STOP → 与团队质疑架构 → 不要继续打补丁
```

---

## 第四层：部署运维规范

### 31. 发布流程

```
T-2 天：代码冻结 + CI 全部通过
T-1 天：建 release 分支 + PR 审核（≥2人）+ 合并 master + CI/CD 部署 staging
发布：灰度 10% → 30% → 50% → 100%（每阶段观察 ≥10 分钟）
发布后：30 分钟高频监控 + 通知 + 更新 CHANGELOG
```

### 32. 灰度与回滚

```
灰度条件（进入下一阶段）：
  ✅ 错误率 < 0.1%
  ✅ P99 < 基准 × 1.2
  ✅ 无 P0/P1 告警

自动回滚触发：错误率 > 1% 或 P99 > 500ms 或任意 P0
回滚操作：kubectl rollout undo deployment/{name}（15 分钟内）
```

### 33. CI/CD 流水线

```
Stage 1：Lint + 类型检查 + 循环依赖检测
Stage 2：单元测试（≥80%）+ 集成测试
Stage 3：SAST + 依赖漏洞扫描 + Secret 检测
         High/Critical 漏洞 → Pipeline 失败
Stage 4：Docker 镜像构建 + 推送仓库
Stage 5：部署测试 + 自动化验收 + 部署 staging

❌ 禁止跳过任何 Stage
❌ 禁止手动修改 CI 配置绕过检查
```

### 34. K8s 加固标准

```
系统加固（必须全部启用）：
  User/Group：非 root 用户
  NoNewPrivileges：true
  ProtectSystem：strict
  ProtectHome：true
  PrivateTmp：true
  SystemCallFilter：@system-service
  ReadOnlyRootFilesystem：true（如可行）
  ResourceQuota：设置 CPU/内存上限

健康检查：
  Liveness：/healthz（失败重启）
  Readiness：/readyz（失败停止流量）
```

### 35. 监控四大支柱

```
Metrics（Prometheus）：QPS / 错误率 / P50,P90,P99 / CPU / 内存
Tracing（Jaeger）：trace_id 全链路透传
Logging（Fluentd→Kafka/ES）：JSON格式，保留≥30天（审计≥180天）
Health（/healthz + /readyz）：存活 + 就绪（含依赖检查）
```

### 36. 告警分级

```
P0 紧急（立即处理）：服务不可用 / 核心功能故障 → 电话+短信+IM
P1 高优（15分钟）：错误率>1% / P99>500ms → IM+电话
P2 中优（当天）：错误率>0.5% / P99>300ms → IM
P3 低优（3天）：一般告警 → IM
```

### 37. 日志规范

```
格式：JSON（结构化）
必须字段：timestamp / level / trace_id / module / message

禁止记录：
  ❌ 密码 / Token / 密钥
  ❌ 手机号 / 身份证（用 * 脱敏）
  ❌ 信用卡号 / CVV

错误日志必须包含：WHAT + WHERE + WHEN + CONTEXT（trace_id / user_id / 脱敏参数）
```

### 38. SLA 计算

```
可用性 = (总时间 - 不可用时间) / 总时间

常见 SLA：
  99.9%（3个9）：年不可用 < 8.7 小时
  99.95%（4个9）：年不可用 < 4.4 小时
  99.99%（5个9）：年不可用 < 53 分钟

告警阈值 = SLA目标 × 0.8（如 SLA 99.9% → 告警 99.72%）
错误预算消耗 > 50% → 冻结非关键发布
错误预算消耗 > 100% → 立即止损
```

---

## 附录：文件索引

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     《软件产品研发必读标准》文件索引                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  L1 架构红线（10条核心强制标准）                                              │
│  └── 架构分层 / 依赖管控 / 三大基础模块 / 数据边界 / 安全基线                 │
│      接口规范 / 事件通信 / 命名规范 / 版本管理 / 质量门槛                     │
│                                                                             │
│  L2 架构设计规范（9条）                                                       │
│  ├── L2A：架构设计标准（评审门槛/模块划分/复用评估/容错/可观测/配置）         │
│  ├── L2B：API设计标准（RESTful/错误码/事件格式/SDK/BC）                      │
│  └── L2C：数据建模标准（DDD/数据库选型/表结构/索引/事务/迁移）               │
│      追加：等保定级预测 / 等保二级清单 / 等保三级增强                        │
│                                                                             │
│  L3 开发实施规范（4条）                                                       │
│  ├── L3A：编码规范（Go/Python/TS/安全编码/Git/注释/API注释）                 │
│  ├── L3B：测试规范（测试金字塔/TDD/集成/回归/性能）                          │
│  └── L3C：调试方法论（根因→模式→假设→修复→架构质疑）                        │
│      追加：代码注释规范（文件头/函数/业务逻辑/TODO）                         │
│                                                                             │
│  L4 部署运维规范（2条）                                                       │
│  ├── L4A：部署运维标准（发布流程/灰度/回滚/CI-CD/K8s加固）                  │
│  └── L4B：监控告警标准（四大支柱/RED+USE/告警分级/日志/SLA）                │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘

来源文档：
├── L1_CORE.md       → 第一层（架构红线）
├── L2_ARCH.md       → 第二层（架构设计 + 等保）
├── L2_API.md        → 第二层（API设计）
├── L2_DATA.md       → 第二层（数据建模）
├── L3_CODE.md       → 第三层（编码 + 注释）
├── L3_TEST.md       → 第三层（测试）
├── L3_DEBUG.md      → 第三层（调试）
├── L4_DEPLOY.md     → 第四层（部署）
└── L4_MONITOR.md    → 第四层（监控）

本文档为综合精简版，编号：ESD-MANDATORY-001-v1.0
```

---

**违反以上任何一条视为违规，产出无效，必须整改后重新审查。**
**本文档为《软件产品研发必读标准》体系核心文件，编号：ESD-MANDATORY-001。**
